<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevinsolano.firebase_tutorial">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GACY"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InicioAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AnfitrionLoginActivity" />
        <activity android:name=".CiclistaLoginActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CiclistaMapsActivity"
            android:label="@string/title_activity_ciclista_maps" />
        <activity
            android:name=".AnfitrionMapsActivity"
            android:label="@string/title_activity_anfitrion_maps" />
        <activity
            android:name=".MenuDrawerActivity"
            android:label="@string/title_activity_menu_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CiclistaRegistroActivity" />
        <activity android:name=".PerfilCiclistaActivity" />
        <activity android:name=".InformacionGaraje" />
        <activity android:name=".PerfilAnfitrionActivity" />
        <activity android:name=".MenuAnfitrionActivity" />
        <activity android:name=".ConfigurarReservaActivity" />
        <activity android:name=".InformacionReservaActivity" />
        <activity android:name=".ReservasActivity" />
        <activity android:name=".CalificarActivity" />
        <activity android:name=".PagoActivity" />
        <activity android:name=".MostrarReservasActivity"></activity>
    </application>

</manifest>